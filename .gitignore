**.jsonl
!public/data/**/*.jsonl
public/data/

# Auditor-produced annotations mirrored from the UI (+ any local-agent records).
# Mutable working output, regenerated by the running dev server — see AGENTS.md.
# These now live inside each trace's own dir at <trace>/audit/; for dropped traces
# that's public/data/<trace>/audit/, already covered by the public/data/ rule
# above. The legacy repo-root /audit/ is kept ignored for pre-move leftovers.
/audit/

CLAUDE.md

.claude/settings.local.json

node_modules
dist
.vite

docs_local
.claude/



docs/*
!docs/readme instructions/
